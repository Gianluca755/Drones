% test 1

c(manager). c(broker). c(client). c(utils).

manager:start().

% create client
client:start(ID, primaryBroker, bckBroker    ).

% send msg makeOrder to client.
Pid ! makeOrder.


% make order but no drones



% make order with some drones
